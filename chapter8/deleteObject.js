let list = {
    name: "Bennison",
    town_name: "coimbature"
};

console.log(list);
delete (list.town_name);   // delete keyword is used to delete object property
console.log(list);

//checking github pull requet
