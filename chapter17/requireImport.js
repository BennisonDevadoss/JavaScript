let { person1, person2 } = require("./addFunToProto")

console.log(person1);
console.log(person2);    // Now just I console it But it not only print the person2 objects but also the export file will be executed ????????

// Importing object....