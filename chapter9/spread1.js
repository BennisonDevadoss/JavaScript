function addition(a, b) {
    return a + b;
}

let myarr = [1, 3, 5];

console.log(addition(...myarr));  // This time we use the spread operator. 

// the spread operator pass the first two array index. 

// What is spread opertor
//Ans: 
//A spread operator takes the group (array, object) and it tries to spread it into the multiple value. 


// In the above example the spread operator (...myarr) pass the value to function as a individual value. 

