const array1 = ["a", "b", "c", "d"];
const array2 = ["e", "f", "g", "i"];
const array3 = array1.concat(array2);


// Concat() method is used to merge two or more arrays. this method does not change thei existing arrays, but instead returns a new array. 

console.log(array3);
// [a, b, c, d, e, f, g, h, i]; 

/* ctrl - shift - a */
/* concat()
concat(value0)
concat(value0, value1)
concat(value0, value1, value2, .... valueN) */

